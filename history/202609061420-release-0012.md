# Prepare gen-code 0.0.12

- Read the project-local Calcit upgrade and agent guides before Snapshot changes.
- Align Calcit and `@calcit/procs` at 0.13.77, with Reel 0.6.19, Respo UI 0.7.19, and Respo 0.16.95.
- Keep `calcit.cirru` as the only source Snapshot; no `compact.cirru` is tracked.
- Retain the nominal `use-gen-code` plugin contract and typed Reel state boundary from current main, with zero unresolved dynamic method dispatch.
- Replace deprecated `tag-match` calls with `match` and add a reviewed static quality baseline.
- Validate strict Caps resolution, immutable Yarn, exact toolchain versions, zero preprocessing warnings, zero dynamic method dispatches, zero deprecated calls, the Reel boundary test, stream-contract tests, JavaScript generation, and the Node 24 / Vite 8.2.2 production build.
