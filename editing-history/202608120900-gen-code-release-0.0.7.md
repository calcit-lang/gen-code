# 2026-08-12 09:00 gen-code 0.0.7 release

- Fast-forwarded the local repository to the latest `origin/main` (type-cleanup and CI snapshot updates).
- Synchronized the Calcit snapshot and npm package version to `0.0.7`.
- Verified `yarn install --immutable`, built-in `cr test` (no attached tests), and `cr --check-only`.
- Release follows the repository's release-branch, PR, merge, tag, and GitHub release workflow.
